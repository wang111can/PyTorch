# Softmax function
Suppose $Z^i \in R^K$ is the output of the last linear layer, the Softmax function is 

$P(y=i)=\frac{e^{Z_i}}{\sum^{K-1}_{j=0}e^{Z_j}}, i \in \{0, \dots, K - 1\}$
$\sum^{K-1}_{j=0}P(j) = 1$

# Loss function


$-y logY_{pred}$